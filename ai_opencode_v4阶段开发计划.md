# tkzs-structlog V4.0 阶段开发计划

> 版本：V4.0
> 创建日期：2026-05-08
> 依据文档：开发需求.md §五/§六、开发计划.md §六/V4.0
> 本次范围：仅 PGSQL（P0）+ Redis（P1）

---

## 一、V4.0 阶段概述

### 1.1 阶段定位

V4.0 生态版定位为**第三方输出适配**，本次实现 PGSQL 和 Redis 两种输出源。

### 1.2 核心功能

| 功能 | 优先级 | 模块 | 说明 |
|------|--------|------|------|
| PGSQL 输出 | P0 | C2 | 批量写入 + 异步队列 + 连接池 |
| Redis 输出 | P1 | C2 | 批量写入 + 异步队列 + List |

### 1.3 技术要求

- 敏感信息从 `.env` 文件读取
- 连接失败自动降级到文件输出
- 异步队列批量写入，不阻塞主日志流程
- 测试覆盖率 100%，通过率 100%

---

## 二、环境准备

### 2.1 依赖安装

| 功能 | 工作内容 | 验收标准 |
|------|----------|----------|
| PGSQL 依赖 | 安装 psycopg2-binary | 可正常 import |
| Redis 依赖 | 安装 redis-py | 可正常 import |
| python-dotenv | 安装 dotenv 用于读取 .env | 可正常 import |

### 2.2 .env 文件支持

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| 环境变量加载 | 新增 dotenv 读取 .env 文件 | .env 中的配置可被读取 |
| PGSQL 配置 | PG_HOST, PG_PORT, PG_USER, PG_PASSWORD, PG_DB | 配置正确读取 |
| Redis 配置 | REDIS_HOST, REDIS_PORT, REDIS_PASSWORD, REDIS_DB | 配置正确读取 |

---

## 三、PGSQL 输出模块 (C2)

### 3.1 配置模型

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| PGSQL 配置段 | 在 validator.py 新增 HandlerPgsqlConfig | 配置可解析 |
| V4 配置集成 | 在 StructlogV4Config 中添加 pgsql 字段 | 版本兼容 |

### 3.2 连接管理

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| 连接池 | 使用 psycopg2.pool.ThreadedConnectionPool | 连接池正常 |
| 连接异常处理 | 连接失败自动重连 3 次 | 重连逻辑正常 |
| 降级处理 | 连接失败降级到文件输出 | 降级生效 |

### 3.3 数据写入

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| 异步队列 | 使用 queue.Queue 缓冲日志 | 队列正常 |
| 批量写入 | 达到批量阈值或定时刷新写入 | 批量写入正常 |
| 表结构 | 自动创建默认表（log_time, level, logger, message, extra） | 表创建正常 |

### 3.4 处理器实现

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| PGSQLHandler 类 | 实现日志写入逻辑 | 日志写入正常 |
| setup_pgsql_handler | 集成到初始化流程 | 集成正常 |

---

## 四、Redis 输出模块 (C2)

### 4.1 配置模型

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| Redis 配置段 | 在 validator.py 新增 HandlerRedisConfig | 配置可解析 |
| V4 配置集成 | 在 StructlogV4Config 中添加 redis 字段 | 版本兼容 |

### 4.2 连接管理

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| 连接池 | 使用 redis.ConnectionPool | 连接池正常 |
| 连接异常处理 | 连接失败自动重连 3 次 | 重连逻辑正常 |
| 降级处理 | 连接失败降级到文件输出 | 降级生效 |

### 4.3 数据写入

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| 异步队列 | 使用 queue.Queue 缓冲日志 | 队列正常 |
| List 结构 | 使用 RPUSH 写入日志 | List 写入正常 |
| Key 前缀 | 支持自定义 key 前缀配置 | 前缀配置生效 |

### 4.4 处理器实现

| 功能点 | 工作内容 | 验收标准 |
|--------|----------|----------|
| RedisHandler 类 | 实现日志写入逻辑 | 日志写入正常 |
| setup_redis_handler | 集成到初始化流程 | 集成正常 |

---

## 五、测试计划

### 5.1 单元测试

| 测试项 | 覆盖模块 | 验收标准 |
|--------|----------|----------|
| PGSQL 配置解析 | validator.py | 覆盖率 100% |
| PGSQL 连接管理 | pgsql_handler.py | 覆盖率 100% |
| PGSQL 批量写入 | pgsql_handler.py | 覆盖率 100% |
| Redis 配置解析 | validator.py | 覆盖率 100% |
| Redis 连接管理 | redis_handler.py | 覆盖率 100% |
| Redis 批量写入 | redis_handler.py | 覆盖率 100% |
| 降级逻辑 | handlers.py | 覆盖率 100% |
| 环境变量读取 | env_loader.py | 覆盖率 100% |

### 5.2 集成测试

| 测试项 | 验收标准 |
|--------|----------|
| PGSQL 端到端 | 日志写入 PGSQL 成功 |
| Redis 端到端 | 日志写入 Redis 成功 |
| 降级集成 | 连接失败时降级到文件 |

### 5.3 边界测试

| 测试项 | 验收标准 |
|--------|----------|
| PGSQL 连接超时 | 降级生效 |
| PGSQL 批量阈值边界 | 边界写入正常 |
| Redis 连接超时 | 降级生效 |
| Redis 批量阈值边界 | 边界写入正常 |
| .env 文件缺失 | 使用默认/空值 |

---

## 六、开发任务拆分

### Sprint 1: 环境准备

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T1.1 安装依赖 | pip install psycopg2-binary redis python-dotenv | 0.5h | 依赖可用 |
| T1.2 环境变量读取模块 | 新增 env_loader.py 读取 .env | 2h | 读取正常 |

### Sprint 2: PGSQL 配置模型

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T2.1 配置模型 | 新增 HandlerPgsqlConfig | 1h | 配置可解析 |
| T2.2 配置集成 | 集成到 StructlogV4Config | 0.5h | 版本兼容 |

### Sprint 3: PGSQL 处理器实现

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T3.1 连接管理 | 实现连接池 + 重连逻辑 | 2h | 连接正常 |
| T3.2 异步队列 | 实现日志缓冲队列 | 2h | 队列正常 |
| T3.3 批量写入 | 实现批量插入逻辑 | 2h | 写入正常 |
| T3.4 表结构 | 自动创建默认表 | 1h | 表创建正常 |

### Sprint 4: Redis 配置模型

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T4.1 配置模型 | 新增 HandlerRedisConfig | 1h | 配置可解析 |
| T4.2 配置集成 | 集成到 StructlogV4Config | 0.5h | 版本兼容 |

### Sprint 5: Redis 处理器实现

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T5.1 连接管理 | 实现连接池 + 重连逻辑 | 2h | 连接正常 |
| T5.2 异步队列 | 实现日志缓冲队列 | 2h | 队列正常 |
| T5.3 List 写入 | 实现 RPUSH 写入 | 1h | 写入正常 |

### Sprint 6: 降级与集成

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T6.1 降级逻辑 | 连接失败降级到文件 | 2h | 降级生效 |
| T6.2 初始化集成 | 集成到 init_structlog | 1h | 集成正常 |

### Sprint 7: 测试与验收

| 任务 | 工作内容 | 工时 | 验收标准 |
|------|----------|------|----------|
| T7.1 单元测试 | 覆盖率 100% | 4h | 覆盖率 100% |
| T7.2 集成测试 | 端到端测试 | 2h | 通过率 100% |
| T7.3 代码质量 | ruff 格式化 + 校验 | 1h | 零错误 |

---

## 七、里程碑验收

### M1: 环境准备完成

- [ ] 依赖安装完成
- [ ] 环境变量读取模块完成

### M2: PGSQL 功能完成

- [ ] 配置模型完成
- [ ] 连接管理完成
- [ ] 异步队列完成
- [ ] 批量写入完成
- [ ] 覆盖率 100%

### M3: Redis 功能完成

- [ ] 配置模型完成
- [ ] 连接管理完成
- [ ] 异步队列完成
- [ ] List 写入完成
- [ ] 覆盖率 100%

### M4: 最终验收

- [ ] 降级逻辑完成
- [ ] 集成测试通过
- [ ] ruff 零错误
- [ ] 全量测试 100% 通过

---

## 八、文件变更清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `src/tkzs_structlog/config/validator.py` | 修改 | 新增 PGSQL/Redis 配置模型 |
| `src/tkzs_structlog/extensions/pgsql_handler.py` | 新建 | PGSQL 输出处理器 |
| `src/tkzs_structlog/extensions/redis_handler.py` | 新建 | Redis 输出处理器 |
| `src/tkzs_structlog/config/env_loader.py` | 新建 | 环境变量读取模块 |
| `tests/unit/test_pgsql_handler.py` | 新建 | PGSQL 单元测试 |
| `tests/unit/test_redis_handler.py` | 新建 | Redis 单元测试 |
| `tests/unit/test_env_loader.py` | 新建 | 环境变量读取测试 |
| `tests/integration/test_v4_integration.py` | 新建 | V4 集成测试 |

---

*文档版本：V1.0*
*最后更新：2026-05-08*