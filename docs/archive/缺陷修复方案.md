# 缺陷修复方案

> **创建日期**: 2026-05-08
> **依据**: 代码审计综述报告 + 开发需求.md (V2.1)

---

## 当前状态

| 指标 | 目标 | 当前 | 差距 |
|------|------|------|------|
| 测试通过率 | 100% | 99.7% (363/364) | 1个失败 |
| 代码覆盖率 | 100% | 90% | 10%缺口 |
| ruff规范 | 0错误 | 13错误(仅测试文件) | 13 |
| mypy检查 | 0错误 | 25+错误 | 25+ |

---

## P0 级别问题（必须立即修复）

### P0-1: 标准logging桥接功能不可用

| 属性 | 内容 |
|------|------|
| **严重程度** | 阻断级 |
| **涉及文件** | `src/tkzs_structlog/core/initializer.py:90-166` |
| **需求依据** | 开发需求.md 第119行 — "bridge_std_logging=True 时调用 structlog.stdlib.recreate_defaults()" |
| **现状** | 代码未调用 `recreate_defaults()`，导致 `logging.getLogger().info()` 无输出处理器 |
| **测试证据** | `tests/integration/test_stdlib_bridge.py::TestStdlibBridge::test_stdlib_logging_emits_message` 失败 |

**修复方案**: 在 `_setup_processors` 中 bridge=True 分支调用 `structlog.stdlib.recreate_defaults(...)`

---

### P0-2: PGSQL批量写入日志计数Bug

| 属性 | 内容 |
|------|------|
| **严重程度** | 高 |
| **涉及文件** | `src/tkzs_structlog/extensions/pgsql_handler.py:210-213` |
| **问题代码** | `self._batch.clear()` 先执行后，`len(self._batch)` 永远为0 |
| **对比** | `redis_handler.py:168-171` 同逻辑正确实现 |

**修复方案**: 将日志输出语句移到 `clear()` 之前，或先保存 `batch_size`

---

### P0-3: mypy类型检查严重错误

| 属性 | 内容 |
|------|------|
| **严重程度** | 高 |
| **错误数量** | 25+ 个 |
| **典型问题** | |
| — Returning Any 违反类型声明 | 9处 (parser.py, env_loader.py等) |
| — None 属性访问 | 5处 (pgsql_handler.py, redis_handler.py) |
| — 类型注解错误 | 3处 (rotation.py, processors.py) |
| — watchdog 类型问题 | 3处 (hotreload.py) |
| — Type 缺泛型参数 | 2处 (processors.py) |

**修复方案**: 逐一修复类型注解，确保mypy检查通过

---

### P0-4: SQL注入风险

| 属性 | 内容 |
|------|------|
| **严重程度** | 中-高（安全相关） |
| **涉及文件** | `src/tkzs_structlog/extensions/pgsql_handler.py:143-152, 198-207` |
| **问题代码** | `cursor.execute(f"CREATE TABLE IF NOT EXISTS {table_name}...")` |
| **风险** | 表名使用字符串格式化，存在SQL注入风险 |

**修复方案**: 添加表名正则验证（仅允许字母、数字、下划线）

---

## P1 级别问题（短期修复）

### P1-1: Python版本配置不一致

| 属性 | 内容 |
|------|------|
| **涉及文件** | `pyproject.toml` |

**修复方案**: 统一更新为 `py310`

---

### P1-2: 控制台处理器未集成

| 属性 | 内容 |
|------|------|
| **涉及文件** | `src/tkzs_structlog/extensions/handlers.py:163-201` |

---

### P1-3: 测试覆盖率不足

| 模块 | 覆盖率 | 主要缺失 |
|------|--------|----------|
| `pgsql_handler.py` | 50% | 核心初始化/写入/刷新未覆盖 |
| `redis_handler.py` | 60% | 缺少真实Redis连接mock |
| `env_loader.py` | 77% | dotenv未安装分支未测试 |

---

## P2 级别问题（中期修复）

### P2-1: CustomRotatingFileHandler未集成

| 属性 | 内容 |
|------|------|
| **涉及文件** | `src/tkzs_structlog/extensions/rotation.py`, `handlers.py` |

---

### P2-2: repr_iter参数未使用

| 属性 | 内容 |
|------|------|
| **涉及文件** | `src/tkzs_structlog/extensions/processors.py:117` |

---

## 修复顺序

1. **P0-1**: 修复stdlib桥接功能（阻断级Bug，测试失败）
2. **P0-2**: 修复PGSQL日志计数Bug
3. **P0-4**: 修复SQL注入风险
4. **P0-3**: 修复mypy类型错误
5. **P1**: 修复Python版本配置和覆盖率问题

---

## 验收标准

- [ ] 测试通过率 100%
- [ ] 代码覆盖率 100%
- [ ] ruff 错误 0
- [ ] mypy 错误 0
